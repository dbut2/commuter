services:
  commuter:
    build: .
    ports:
      - "8080:8080"
    environment:
      - STRAVA_CLIENT_ID=placeholder
      - STRAVA_CLIENT_SECRET=placeholder
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - PORT=8080
    depends_on:
      - redis

  redis:
    image: redis:alpine
